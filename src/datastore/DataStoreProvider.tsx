// Screens : Text-Name, HomeScreen

// takes username -> waits until the fetching is done -> if username is there we let them in

// input -> text -> let user in to home
// text =/= -> upper flow
// text \/ -> don't show login, take to home
